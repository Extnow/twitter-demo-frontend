language: node_js

node_js:
  - "node"

deploy:
  - provider: script
    script: now --token $NOW_TOKEN && now alias --token $NOW_TOKEN
    skip_cleanup: true
    on:
      master: true
      feat/refactoring,add-flow,connect-zeit: true